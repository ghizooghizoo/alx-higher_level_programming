-- lists all records of the table second_table
-- Results should display both the score and the name (in this order)
-- according to high scores 
SELECT `score`, `name` FROM `second_table`
ORDER BY `score` DESC;
